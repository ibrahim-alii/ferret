"""Eval suite runner — stub until Module 6 is integrated."""
from __future__ import annotations

from typing import Optional


async def run_all(paper_id: str, mode: Optional[str] = None) -> dict:
    """Run all evals for a paper; returns summary and report path."""
    return {"summary": "stub", "report_path": ""}
